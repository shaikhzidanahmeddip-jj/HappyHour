#!/usr/bin/env bash
set -euo pipefail

if git grep -nE '^(<<<<<<< |=======|>>>>>>> )' -- '*.cs' '*.csproj' '*.md' >/dev/null; then
  echo "Merge conflict markers detected. Resolve them before committing."
  git grep -nE '^(<<<<<<< |=======|>>>>>>> )' -- '*.cs' '*.csproj' '*.md'
  exit 1
fi
